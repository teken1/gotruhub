import React from "react";
import { Link } from "react-router-dom";
import { MainLayout, WalletContent } from "../../components";

export const Wallet = () => {
  return (
    <MainLayout>
      <WalletContent />
    </MainLayout>
  );
};
