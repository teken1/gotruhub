import React from "react";
import { Link } from "react-router-dom";
import { MainLayout, MessagingContent } from "../../components";

export const Messaging = () => {
  return (
    <MainLayout>
      <MessagingContent />
    </MainLayout>
  );
};
