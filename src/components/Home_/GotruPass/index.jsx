import { Button } from "../../FormElements/Button";
import { Manage } from "../Manage_";

export const GotruPass = () => {
  return (
    <Manage
      img="attendance_"
      title="Gotru Pass"
      text="With this feature, schools/corporate organizations captures resumption you and exit times of students/members of staff in real time, it also sends automatic and instant notifications to parent's/Management's contacts at each capture."
    />
  );
};
